# Oversized File Follow-ups

The files below exceed 800 lines after the current gameplay work. They were already oversized legacy aggregates or compatibility-facade entry points. Splitting them in the same change would combine gameplay fixes with a broad structural rewrite. The next focused refactor for each file is recorded here so the temporary exception has a concrete exit path.

| File | Why it remains oversized in this change | Required extraction follow-up |
| --- | --- | --- |
| `csharp/CombatHUD.cs` | Legacy HUD compatibility facade and shared binding owner. | Move the remaining loot/backpack composition and diagnostics into focused view controllers, leaving only scene binding and delegation. |
| `csharp/CombatHUD.Deployment.cs` | The existing deployment UI partial still combines map selection, deployment persistence, preview state, and session launch; this change only adds a narrow map-selection-changed event used to trigger Jianghai preload and cancellation. | Extract deployment storage and map-selection orchestration into a focused controller, leaving this partial as the view-binding compatibility adapter. |
| `csharp/CombatHUD.Squad.cs` | The existing squad UI partial still combines lobby roster rendering, matchmaking/session status, and deployment handoff; this change only adds a narrow text-only session-status update. | Extract lobby roster and session-status presentation into a focused view/controller with explicit inputs and user-intent outputs. |
| `csharp/CombatModelLibrary.cs` | Legacy authored operator and weapon factories still share resource validation, bounds, sockets, and presentation assembly; this change only makes preview ownership exception-safe. | Move operator construction and its shared bounds helpers into a focused `CombatModelLibrary.Operator.cs` partial, then separate the remaining weapon-family factories without changing their public contracts. |
| `csharp/DemolitionArenaLayout.cs` | The legacy pure-data constructor still selects and assembles every demolition map; this change only replaces Bazaar Crossing's literal clearance samples with values derived from its authored wall gaps. | Extract per-map layout construction into focused immutable layout factories, leaving `DemolitionArenaLayout` responsible for the shared contract, pure balance queries, and map-factory selection. |
| `csharp/EnemyOperator.cs` | Legacy enemy lifecycle, combat, loot, and movement aggregate. | Extract loot acquisition and loadout state into a regular C# controller, then move the remaining authored-visual hooks to the existing visual partial. |
| `csharp/FreightTerminalWorld.Demolition.Device.cs` | The existing device lifecycle partial still combines ownership transfer, pickup eligibility, world presentation, network reconciliation, and squad objective control; this change only closes the autonomous-versus-human carrier boundary and last-window squad commitment. | Extract device ownership/transfer into a pure lifecycle controller and move squad carrier/defuser adaptation into a focused `FreightTerminalWorld.Demolition.SquadObjective.cs` partial. |
| `csharp/FreightTerminalWorld.Demolition.Diagnostics.cs` | Deterministic demolition scenarios still share one fixture lifecycle. | Split round-flow, tactical-route, and economy scenarios into separate diagnostic partial files with one shared setup helper. |
| `csharp/FreightTerminalWorld.Demolition.Network.cs` | The legacy demolition network partial still combines actor replication, phase snapshots, damage relay, session transitions, and match-state application; this change only adds the compact authoritative per-slot weapon snapshot and round-end utility cleanup. | Extract actor-state replication and match-phase snapshot transport into separate focused partials, leaving this file responsible for session lifecycle wiring. |
| `csharp/FreightTerminalWorld.Demolition.Strategy.cs` | The legacy demolition strategy partial still combines snapshot collection, objective-channel ownership, opponent and squad routing, post assignment, recovery, and route execution; this combined routing change must keep site commitment, route-cost inputs, and elevation-aware completion aligned across those responsibilities. | Extract snapshot/plan refresh and carrier-site commitment into a bounded strategy coordinator, move opponent route driving/cursor recovery into `FreightTerminalWorld.Demolition.Navigation.cs`, and leave the partial responsible only for actor adaptation and movement application without changing diagnostic projections. |
| `csharp/FreightTerminalWorld.Demolition.cs` | Compatibility coordinator for the existing demolition mode. | Extract round transitions and spectator state into a bounded demolition match coordinator. |
| `csharp/FreightTerminalWorld.Interdistrict.cs` | Procedural route-network assembly, clearance probes, and its deterministic validator still share one partial. | Move district-network diagnostics into `FreightTerminalWorld.Interdistrict.Diagnostics.cs`, then extract deck/stair generation into a route-network builder service. |
| `csharp/FreightTerminalWorld.Level.cs` | Legacy freight-terminal geometry assembly remains the compatibility entry point for authored props and traversal-link registration. | Extract cover/prop assembly into a freight-terminal level builder, leaving `BuildLevel` responsible only for map selection and service composition. |
| `csharp/FreightTerminalWorld.Residential.cs` | Procedural residential assembly and its legacy diagnostics share one partial. | Move residential gameplay diagnostics into `FreightTerminalWorld.Residential.Diagnostics.cs`, then extract room/cache assembly into a builder service. |
| `csharp/FreightTerminalWorld.Squad.NavGrid.cs` | Grid probing, route-state lifecycle, traversal preservation, and diagnostics-facing projections remain coupled while multi-level rescue is introduced incrementally. | Extract physics sampling/cache into a navigation probe service and move route-state transitions into a regular C# planner/controller. |
| `csharp/FreightTerminalWorld.Squad.Traversal.cs` | The legacy layered-route facade still owns authored-link registration, endpoint attachment, graph planning, and runtime recovery; this change only extends its stable directive contract for explicit ladder actions. | Extract authored traversal-link storage and immutable graph construction into a focused planner service, leaving the world partial responsible for actor/world probes and route delivery. |
| `csharp/FreightTerminalWorld.Squad.cs` | Legacy squad lifecycle, rescue coordination, spectator state, and diagnostics share one partial. | Move the squad validator into `FreightTerminalWorld.Squad.Diagnostics.cs`, then extract AI rescue assignment into a pure coordinator. |
| `csharp/FreightTerminalWorld.cs` | Main compatibility facade still owns legacy interaction and diagnostic entry points. | Extract loot interaction orchestration and the remaining validators into focused partials/controllers, leaving composition and lifecycle delegation. |
| `csharp/SquadMate.cs` | Legacy squad actor aggregate still owns navigation, orders, revive state, and animation. | Extract revive locomotion/state transitions into a focused controller and keep the node as the lifecycle adapter. |
| `csharp/SquadMate.Combat.cs` | Combat movement, formation settling, obstacle avoidance, and stall recovery still share one tactical state machine. | Extract formation-follow and local-avoidance state into a focused locomotion controller while preserving the existing combat target lifecycle. |
| `csharp/TacticalPlayer.cs` | Legacy player aggregate still owns health, inventory, input locks, and weapon state. | Extract downed/interaction-lock state and inventory ownership into focused controllers while preserving existing public signals and save keys. |

Each extraction must land as its own behavior-preserving change and pass the diagnostics named for that subsystem in `AGENTS.md`.

## Task-scoped DCC generators

| File | Why it remains oversized in this change | Required extraction follow-up |
| --- | --- | --- |
| `scripts/blender/build_bazaar_crossing.py` | The first deterministic Bazaar Crossing delivery keeps CC0 source validation, Blender scene composition, packed `.blend`/GLB export, preview rendering, and round-trip budget checks in one background-Blender process so every generated artifact shares one audited scene lifecycle. Splitting that mutable `bpy` pipeline while the map contract is still being established would make the initial asset delivery harder to reproduce and review. | Extract immutable map/source specifications into `bazaar_crossing_specs.py`, reusable source/material/mesh helpers into `bazaar_crossing_dcc.py`, and report/round-trip validation into `bazaar_crossing_validation.py`; leave the entry script responsible only for ordered orchestration and final `BAZAAR_DCC_PASS` reporting. |
